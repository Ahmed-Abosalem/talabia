# Talabia Frontend

واجهة متجر طلبية المبنية باستخدام React + Vite + TailwindCSS.

## تشغيل المشروع

```bash
npm install
npm run dev
```

تأكد من تشغيل خادم الـ Backend على `http://localhost:5000` أو عدّل متغير البيئة `VITE_API_BASE_URL`.
