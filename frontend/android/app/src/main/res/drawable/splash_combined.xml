<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Logo (Centered, shifted slightly up) -->
    <item
        android:drawable="@drawable/splash_logo"
        android:width="120dp"
        android:height="60dp"
        android:gravity="center"
        android:top="-15dp" />

    <!-- Tagline (Centered, shifted slightly down) -->
    <item
        android:drawable="@drawable/splash_tagline"
        android:width="140dp"
        android:height="40dp"
        android:gravity="center"
        android:top="35dp" />
</layer-list>
